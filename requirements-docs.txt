# MkDocs 사이트 빌드용 — GitHub Actions 가 이 목록만 설치합니다.
# 노트북 실행에 필요한 라이브러리(LangChain·Ragas 등) 는 여기에 포함하지 않습니다.
mkdocs==1.6.1
mkdocs-material>=9.5,<10
pymdown-extensions>=10.7
# 강의를 courses/<slug>/ 로 옮기면서 생긴 예전 URL 을 새 경로로 전환 (mkdocs.yml 의 redirects 플러그인)
# 1.2.3 은 mkdocs 를 리네임한 'properdocs' 패키지를 추가로 끌어오므로 1.2.2 에 고정.
mkdocs-redirects==1.2.2
